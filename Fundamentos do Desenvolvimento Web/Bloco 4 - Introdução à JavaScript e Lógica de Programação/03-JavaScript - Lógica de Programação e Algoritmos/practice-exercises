// JavaScript - Lógica de Programação e Algoritmos

// *1- Para o primeiro exercício de hoje, faça um programa que, dado um valor n qualquer, seja n > 1 , imprima na tela um quadrado feito de asteriscos de lado de tamanho n . Por exemplo:
// n = 5

// *****
// *****
// *****
// *****
// *****

      //criar variável ASTERISCOS que acumula os asteriscos e começa com string vazia
      //inserir um looping que começa em 0 e vai até "n"
      //dentro do looping, adicionar um "*" à variável ASTERISCOS .

      //criar outro looping que começa em 0 e tb vai até "n"
      //dentro desse looping imprimir a variável ASTERISCOS ("n" vezes)

      let n = 5; 

      let asteriscos = "";  
      for (let i=0 ; i < n; i +=1){
        asteriscos += "*"
      }

      for (let i=0 ; i < n; i +=1){
      console.log(asteriscos);
      }

      //Tentando executar com apenas um laço FOR ??????????????????      

// 2- Para o segundo exercício, faça o mesmo que antes, mas que imprima um triângulo retângulo com 5 asteriscos de base. Por exemplo:
// n = 5

// *
// **
// ***
// ****
// *****

    //criar variável n e variável ASTERISCOS (com string vazia)
    //iniciar loop que começa em 0 e termina em "n"
    //cada iteração do loop, imprime a variável ASTERISCOS e adiciona "*" na variável.
    
    let n =10;

    let asteriscos = '';

    for (let i = 0 ; i < n ; i += 1 ){
      asteriscos += "*";
      console.log(asteriscos);
    }

// 3- Agora inverta o lado do triângulo. Por exemplo:
// n = 5

//     *
//    **
//   ***
//  ****
// *****

    //criar variável acumuladora ASTERISCOS = 0, variável ESPAÇOS = 5 e variável "n"
    //criar laço que começa com 0 e termina menor que "n"
    //criar outro laço que começa com n e termina >0
    //imprimir ESPAÇOS + ASTERISCOS.


